## Purpose
Core package for the swingmaster rule-based market state engine.

## Key Areas
- `core/`: state machine, policies, signal abstractions.
- `app_api/`: providers and application wiring.
- `cli/`: audit and universe runner tools.
- `infra/`: persistence adapters (SQLite) and ports.
- `tests/`: unit and CLI tests.
