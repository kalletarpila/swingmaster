## Purpose
Provider implementations that emit SignalSet instances from market data.

## Key Areas
- `signals_v2/`: individual signal modules (one signal per file).
- `osakedata_signal_provider_v2.py`: v2 signal provider implementation.
