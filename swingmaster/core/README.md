## Purpose
Deterministic core engine for state transitions, policies, and signal abstractions.

## Key Files
- `engine/evaluator.py`: evaluates one day of signals into Decisions.
- `policy/`: rule-based transition policies and guardrails.
- `signals/`: SignalKey enums and SignalSet container.
- `domain/`: State/Reason enums and core models.
